#pragma once

#include "ofMain.h"

class openGo {
    
public:
    void draw(vector<ofPoint> particles_, vector<ofColor> colors_, GLenum mode_ = GL_POINTS, float lineWidth_ = 4, float pointSize_ = 8);
    
private:
};
